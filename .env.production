# =============================================================================
# Meetily 生产环境变量配置
# =============================================================================
# 复制此文件为 .env 并填入实际值：
#   cp .env.production .env
#
# ⚠️  生产环境必须在服务器上填写所有「your_xxx」占位符的值！
# =============================================================================

# ---------------------------------------------------------------------------
# 运行模式：Docker 部署不要设置 LOCAL_DEV
# 本地开发时设置 LOCAL_DEV=true，config.yaml 中的 Docker 服务名会自动换为 127.0.0.1
# ---------------------------------------------------------------------------
# LOCAL_DEV=true

# ---------------------------------------------------------------------------
# MySQL（Docker Compose 内部使用）
# ---------------------------------------------------------------------------
MYSQL_ROOT_PASSWORD=meetily2026
MYSQL_DATABASE=graduation_db

# ---------------------------------------------------------------------------
# 阿里云 OSS 对象存储（会议录音文件存储）
# ---------------------------------------------------------------------------
OSS_ACCESS_KEY_ID=your_oss_access_key_id
OSS_ACCESS_KEY_SECRET=your_oss_access_key_secret
OSS_BUCKET_NAME=your_bucket_name
OSS_ENDPOINT=oss-cn-beijing.aliyuncs.com

# ---------------------------------------------------------------------------
# JWT 鉴权配置
# ---------------------------------------------------------------------------
JWT_SECRET_KEY=your_very_strong_secret_key_change_it
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# =============================================================================
# 模型与服务配置（.env 优先，未设置时可从数据库 config 表读取）
# 命名规则：{SECTION前缀}_{字段名}，全部大写
# 所有 LLM 模型均通过 chat_model 节配置初始化（使用 OpenAI 兼容 API）
# =============================================================================

# ---------------------------------------------------------------------------
# 对话模型（chat_model）— 用于 AI 对话、纪要生成、总结等
# ---------------------------------------------------------------------------
CHAT_MODEL_BASE_URL=https://your-model-endpoint/compatible-mode/v1
CHAT_MODEL_API_KEY=your_chat_model_api_key
CHAT_MODEL_MODEL=qwen-plus

# ---------------------------------------------------------------------------
# 查询改写模型（rewrite_model）— 用于 RAG 中的用户查询优化
# ---------------------------------------------------------------------------
REWRITE_MODEL_BASE_URL=https://your-model-endpoint/compatible-mode/v1
REWRITE_MODEL_API_KEY=your_rewrite_model_api_key
REWRITE_MODEL_MODEL=qwen-plus

# ---------------------------------------------------------------------------
# Router 模型（router_model）— 用于意图路由，需支持 structured output
# ---------------------------------------------------------------------------
ROUTER_MODEL_BASE_URL=https://your-model-endpoint/compatible-mode/v1
ROUTER_MODEL_API_KEY=your_router_model_api_key
ROUTER_MODEL_MODEL=qwen-plus

# ---------------------------------------------------------------------------
# 语音转写（transcription）— OpenAI 兼容的 ASR 接口
# ---------------------------------------------------------------------------
TRANSCRIPTION_BASE_URL=http://your-asr-endpoint/v1-openai/audio/transcriptions
TRANSCRIPTION_API_KEY=your_transcription_api_key
TRANSCRIPTION_MODEL=Whisper-large-v3

# ---------------------------------------------------------------------------
# DashScope 配置 — 阿里云通义千问 ASR / LLM 官方接口
# ---------------------------------------------------------------------------
DASHSCOPE_API_KEY=your_dashscope_api_key
DASHSCOPE_WORKSPACE_ID=your_workspace_id
DASHSCOPE_BASE_URL=https://your-workspace-endpoint/api/v1

# ---------------------------------------------------------------------------
# 向量嵌入模型（embeddings）— 用于知识库文档向量化
# ---------------------------------------------------------------------------
EMBEDDINGS_BASE_URL=https://api.siliconflow.cn/v1/embeddings
EMBEDDINGS_API_KEY=your_embeddings_api_key
EMBEDDINGS_MODEL=BAAI/bge-m3

# ---------------------------------------------------------------------------
# 重排序模型（rerank）— 用于 RAG 检索结果重排序
# ---------------------------------------------------------------------------
RERANK_BASE_URL=https://api.siliconflow.cn/v1/rerank
RERANK_API_KEY=your_rerank_api_key
RERANK_MODEL=BAAI/bge-reranker-v2-m3

# ---------------------------------------------------------------------------
# HuggingFace（可选）— Mirror 下载或私库访问 Token
# ---------------------------------------------------------------------------
HUGGINGFACE_TOKEN=your_huggingface_token

# ---------------------------------------------------------------------------
# CORS 额外域名（生产环境部署域名，逗号分隔）
# ---------------------------------------------------------------------------
CORS_ORIGINS=
